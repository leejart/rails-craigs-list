<h1>Categories</h1>

<table>
  <tr>
    <th>Category</th>
  </tr>
  <% @categories.each do |category| %>
    <tr>
      <td><%= link_to category.name, category_posts_path(category.id) %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Category', new_category_path %>
